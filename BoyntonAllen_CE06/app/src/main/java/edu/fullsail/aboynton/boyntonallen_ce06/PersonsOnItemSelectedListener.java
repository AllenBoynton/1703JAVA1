// Allen Boynton

// JAV1 - 1703

// PersonsOnItemSelectedListener.java

package edu.fullsail.aboynton.boyntonallen_ce06;

import android.view.View;
import android.widget.AdapterView;

class PersonsOnItemSelectedListener implements AdapterView.OnItemSelectedListener {

    public void onItemSelected(AdapterView<?> parent, View view, int pos, long id) {
//        String list_view = parent.getItemAtPosition(pos).toString();
//        String grid_view = parent.getItemAtPosition(pos).toString();
//        String array_view = parent.getItemAtPosition(pos).toString();
//        String simple_view = parent.getItemAtPosition(pos).toString();
//        String base_view = parent.getItemAtPosition(pos).toString();
    }

    @Override
    public void onNothingSelected(AdapterView<?> arg0) {
        // TODO Auto-generated method stub
    }

}
